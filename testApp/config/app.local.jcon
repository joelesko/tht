{
    // Database settings
    // See: https://tht.help/manual/module/db
    databases: {

        // Default sqlite file in 'data/db'
        default: {
            driver: sqlite
            file: app.db
        }

        errors: {
            driver: sqlite
            file: errors.db
        }

        mapDb: {
            driver: sqlite
            file: mapDb.db
        }

        badDb: {
            driver: badDriver
        }

        mysqlDb: {
            driver: mysql
            server: localhost
            database: test
            username: script
            password: script
        }
    }

    email: {
        host: smtp.sendgrid.net
        port: 587
        username: apikey
        password: <api_key>
    }

    app: {
        local: {
            localVar: 789
        }
    }
}
